Thank you for installing {{ .Chart.Name }}!

Your release is named: {{ .Release.Name }}

1. Get the application URL:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- else }}
  kubectl port-forward svc/{{ include "glyph.fullname" . }}-frontend {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }} -n {{ .Release.Namespace }}
  Then open: http://localhost:{{ .Values.frontend.service.port }}
{{- end }}

2. Check the CNPG database cluster status:
  kubectl get cluster {{ include "glyph.cnpg.clusterName" . }} -n {{ .Release.Namespace }}

3. Check migration job status:
  kubectl get jobs -l app.kubernetes.io/component=migrate -n {{ .Release.Namespace }}

{{- if not .Values.api.oidc.enabled }}

WARNING: OIDC authentication is disabled. The API is running in dev mode.
Set api.oidc.enabled=true and configure the OIDC provider for production use.
{{- end }}
