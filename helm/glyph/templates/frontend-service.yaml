apiVersion: v1
kind: Service
metadata:
  name: {{ include "glyph.fullname" . }}-frontend
  labels:
    {{- include "glyph.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glyph.frontend.selectorLabels" . | nindent 4 }}
