name: CD

on:
  push:
    branches: [main]
    paths-ignore:
      - "src-tauri/**"
      - ".github/workflows/release-desktop.yml"
      - ".github/workflows/prepare-desktop-release.yml"
      - ".github/workflows/tag-desktop-release.yml"

permissions:
  contents: read

jobs:
  deploy:
    if: github.repository == 'imaustink/glyph'
    runs-on: arc-runner-set
    steps:
      - uses: actions/checkout@v4

      # Mint a short-lived token scoped to the private homelab repo so we can
      # fetch the production Helm values (which live outside this repo).
      - name: Generate homelab access token
        id: homelab-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.HOMELAB_APP_ID }}
          private-key: ${{ secrets.HOMELAB_APP_PRIVATE_KEY }}
          owner: imaustink
          repositories: homelab

      - name: Checkout homelab (production values)
        uses: actions/checkout@v4
        with:
          repository: imaustink/homelab
          token: ${{ steps.homelab-token.outputs.token }}
          path: homelab
          persist-credentials: false

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Install kubectl
        uses: azure/setup-kubectl@v4

      - name: Sync migrations
        run: |
          rm -rf helm/glyph/migrations
          cp -r api/migrations helm/glyph/migrations

      # The image for this commit's content was usually built and pushed by
      # ci.yml's build-and-push job when it ran on the PR that got merged —
      # tagged by git tree hash rather than commit SHA, since the PR branch's
      # commit SHA and this merge commit's SHA differ (unless every merge is
      # a fast-forward), but the tree hash is identical on both as long as
      # the file contents are unchanged. So deploy just references that tag.
      - name: Resolve image tag
        id: image
        run: echo "tag=$(git rev-parse HEAD^{tree})" >> "$GITHUB_OUTPUT"

      - name: Docker login
        run: |
          echo "$REGISTRY_PASSWORD" | docker login registry.kurpuis.com:5000 \
            -u "$REGISTRY_USERNAME" --password-stdin
        env:
          REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
          REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}

      # ...but not always: a PR from a fork gets no secrets on `pull_request`,
      # so ci.yml skips build-and-push there and the tag is missing. Build it
      # here instead. Fork code only ever reaches a registry credential after
      # a maintainer has reviewed and merged it. Build args must stay in sync
      # with ci.yml's build-and-push so both paths produce the same image.
      - name: Build & push missing images
        run: |
          tag=${{ steps.image.outputs.tag }}

          if docker manifest inspect \
            registry.kurpuis.com:5000/glyph-frontend:"$tag" >/dev/null 2>&1; then
            echo "frontend:$tag already in registry"
          else
            echo "frontend:$tag missing — building"
            docker build \
              --build-arg VITE_STORAGE_MODE=api \
              --build-arg VITE_API_URL= \
              -t registry.kurpuis.com:5000/glyph-frontend:"$tag" \
              -f Dockerfile .
            docker push registry.kurpuis.com:5000/glyph-frontend:"$tag"
          fi

          if docker manifest inspect \
            registry.kurpuis.com:5000/glyph-api:"$tag" >/dev/null 2>&1; then
            echo "api:$tag already in registry"
          else
            echo "api:$tag missing — building"
            docker build \
              -t registry.kurpuis.com:5000/glyph-api:"$tag" \
              -f api/Dockerfile api/
            docker push registry.kurpuis.com:5000/glyph-api:"$tag"
          fi

      - name: Helm upgrade
        run: |
          helm upgrade glyph helm/glyph \
            -f homelab/kubernetes/helm/deployments/glyph/values-production.yaml \
            --set frontend.image.tag=${{ steps.image.outputs.tag }} \
            --set api.image.tag=${{ steps.image.outputs.tag }} \
            -n glyph --timeout 5m

      - name: Rollout restart
        run: |
          kubectl rollout restart deploy/glyph-frontend deploy/glyph-api -n glyph
          kubectl rollout status deploy/glyph-frontend deploy/glyph-api -n glyph --timeout=5m
